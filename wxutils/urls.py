from django.conf.urls import url
# from wxutils.views import WxLoginView

urlpatterns = [
    # url(r'^wxlogin/$', WxLoginView.as_view(), name='wx_login'),
    # url(r'^wxbizdatacyrt/$', WxLoginView.as_view(), name="wxbizdatacyrt" ),
]